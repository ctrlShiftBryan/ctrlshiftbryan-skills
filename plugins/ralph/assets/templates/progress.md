# progress.md

Per-loop log. Ralph appends a block per finished item (newest at bottom):

```
## <item-id> <sha>
files: <comma-separated paths>
decided: <one-line — non-obvious choices>
blockers: <one-line, or "none">
```

Skim, don't re-read in full. The git log is the canonical record; this file
is a fast-path so future loops can skip exploration.

---
